# Environment variables for cospec-ai with mem0 integration

# Server configuration
PORT=3001
NODE_ENV=development

# Markdown directory configuration
MARKDOWN_DIR=/markdown

# API security
API_KEY=demo-api-key

# mem0 configuration (uses mem0 library with configurable vector store)
MEM0_API_KEY=your-mem0-api-key
MEM0_USER_ID=default-user
MEM0_MODEL=gpt-3.5-turbo
VECTOR_STORE_PROVIDER=pgvector
EMBEDDER_MODEL=text-embedding-3-small
PGVECTOR_HOST=localhost
PGVECTOR_PORT=5432
PGVECTOR_DATABASE=mem0
PGVECTOR_USER=mem0user
PGVECTOR_PASSWORD=mem0pass

# Optional: For local LLM with Ollama
# OLLAMA_API_URL=http://localhost:11434/api